# Database
# Local development: SQLite file in the db/ directory
# Vercel: set via vercel.json (file:./db/custom.db, seeded at build time)
# Production with persistence: use Turso, Neon, or Supabase and update prisma/schema.prisma
DATABASE_URL="file:./db/custom.db"
